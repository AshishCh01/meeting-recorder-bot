"""
The Ask AI agent: the per-request instruction (instruction.py), stored
conversations (history.py), chat titles (titles.py) and the streaming turn
that ties them to the shared agent loop (service.py).
"""
