# Development and test dependencies.
#
# Installed by backend/Dockerfile.dev (local docker-compose) and by anyone
# running the suite on the host. backend/Dockerfile - the image
# docker-compose.prod.yml deploys - installs requirements.txt only, so none
# of this ships to the server.
#
#   pip install -r requirements-dev.txt
#
-r requirements.txt

# Test runner for backend/tests - see tests/README.md.
pytest==9.1.1
