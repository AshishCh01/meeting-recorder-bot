# Dependencies
node_modules/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
pip-log.txt
pip-delete-this-directory.txt

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# Build outputs
dist/
build/
.next/
out/
.vite/

# Temporary files & logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.direnv/

# Editor/OS noise (frontend/.gitignore and the *.dockerignore files
# already have this - added here too since backend/ and meeting-bot/
# have no .gitignore of their own and rely on this root one)
.vscode/
.idea/

# Project specifics
recordings/
!recordings/.gitkeep
# The second recorder's kept recordings (docs/scaling-plan.md Phase C3).
recordings-2/
!recordings-2/.gitkeep
browser-profiles/
transcript.json
test_file.ps
backend/test.db

# debug screenshots - these capture real meeting content (see
# DEBUG_SCREENSHOTS in meeting-bot/.env.example) and must never be
# committed regardless of what filename a future selector-hunting
# session happens to write.
meeting-bot/*.png
zoom-admission-timeout.png


google-meet-prejoin.png
google-meet-after-join-click.png
google-meet-admission-timeout.png
google-meet-alone.png

repomix-output.xml

auth.json
zoom-auth.json

meeting-bot/auth.json
meeting-bot/zoom-auth.json

# Audit reports that may quote real secret values found in scans
docs/secrets-audit.md

# Fill-in-the-blank sheet with real .env values, for pasting into the
# Render dashboard - never commit this
docs/render-env-values.md


backend/.env.local

E:\meeting-recorder-bot\backend\benchmark_auth.py